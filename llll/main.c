#include <stdio.h>
#include <stdlib.h>
#include <locale.h>


int main() {
    setlocale(LC_ALL, "Portuguese_Brazil");

    int a[3], b[3], c[3];
    int soma1 = 0, soma2 = 0, a1 = 0, a2 = 0;

    for(int i = 0; i < 3; i++){
        printf("digite o Valor de A: ");
        scanf("%d", &a[i]);

        printf("digite o Valor de B: ");
        scanf("%d", &b[i]);

        printf("digite o Valor de C: ");
        scanf("%d", &c[i]);
                1           2
        if(maiorsalario[i] < salario && sexo[i] == 1 && filho[i] == 0){
            soma2 = a[i] + b[i];
            a1++;
            50 && 0 && 2
    }
    printf("o valor de C é: %d", soma1);
    printf("\no valor de C é: %d", soma2);
    printf("\nquantidade 1: %d",a1 );
    printf("\nquantidade 2: %d",a2);



    return 0;
}
